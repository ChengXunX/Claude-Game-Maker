---
name: incremental-server-extension
description: 增量扩展已有服务端 — 当上游设计大于当前实现时，按依赖顺序逐个扩展DB schema、seed数据、路由、测试，而非重写
trigger: 服务端扩展,增量开发,extend server,add endpoints,add routes,扩展API,新增接口,upstream design bigger than current
examples: 
---

当需要根据新的设计文档扩展现有服务端时，按以下步骤执行：